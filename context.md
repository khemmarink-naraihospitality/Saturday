# สรุปหน่วยความจำ Logic การ Import ข้อมูลแบบ Dynamic (NHGOne)

เอกสารฉบับนี้เป็นแหล่งข้อมูลอ้างอิงหลัก (Single Source of Truth) สำหรับตรรกะการนำเข้าข้อมูลจาก Excel ซึ่งได้รับการทดสอบและยืนยันร่วมกับคุณอุ้ย (Ui) เรียบร้อยแล้ว

---

## 1. การระบุข้อมูลบอร์ด (Board Identification)
- **Title (A1):** ชื่อบอร์ดจะถูกดึงจากเซลล์ A1 โดยระบบจะลบตัวเลขนำหน้า (เช่น "1) ", "2.1) ") ออกให้อัตโนมัติเพื่อให้ได้ชื่อที่สะอาด
- **Description (A2):** คำอธิบายบอร์ดจะถูกดึงจากเซลล์ A2 (ถ้ามี)
- **Import Statistics:** ระบบจะคำนวณและแสดงจำนวนบอร์ด, กลุ่ม, รายการ, และ "จำนวนการอัปเดต" ที่ตรวจพบในหน้าสรุปก่อนกดยืนยัน

## 2. ลำดับและการตรวจหาคอลัมน์ (Column Order & Detection)
- **Preserve Order:** ระบบจะสแกนหัวตารางจากซ้ายไปขวาตามไฟล์ Excel จริงเท่านั้น เพื่อให้ลำดับคอลัมน์ในบอร์ดตรงกับต้นฉบับ
- **ประเภทคอลัมน์ (Type Mapping):**
  - **People:** ใช้คำว่า "Responsible", "User ID"
  - **Text (Special):** คอลัมน์ "Person" และ "Owner" จะถูกบังคับให้เป็น **Text** เพื่อความแม่นยำในการแสดงผลชื่อตามต้นฉบับ
  - **Status:** ใช้คำว่า "Status", "Complete", "Approved", "Sent"
  - **Timeline:** ใช้คำว่า "Timeline" (ดูรายละเอียดการรวมคอลัมน์ในข้อ 3)
  - **Number:** ใช้คำว่า "Cost", "Budget", "Amount", "Number"
  - **Date:** ใช้คำว่า "Date"
  - **Files:** ใช้คำว่า "File", "Quote"
  - **Other:** หากไม่ตรงกับเงื่อนไขใดๆ จะถูกกำหนดเป็น **Text** โดยปริยาย

## 3. การรวม Timeline (Aggressive Timeline Pairing)
- **Logic:** เมื่อเจอคอลัมน์ที่มีคำว่า **"Timeline"** และ **"Start/เริ่ม"** ระบบจะมองหาคอลัมน์ถัดไปที่มีคำว่า **"Timeline"** และ **"End/จบ"** ที่ใกล้ที่สุดและยังว่างอยู่
- **Merging:** ทั้งคูจะถูกรวมเข้าเป็นประเภท Timeline คอลัมน์เดียว โดยชื่อคอลัมน์จะดึงมาจากคอลัมน์ Start (เช่น "2022 Timeline") และข้อมูลข้างในจะเป็น Object `{ from, to }`

## 4. โครงสร้างข้อมูล (Data Hierarchy & Hybrid Support)
- **Groups:** ตรวจจับแถวที่มีคำว่า "Priority" หรือแถวที่แสดงชื่อกลุ่มใหม่
- **Subitems:** ตรวจจับคำว่า "Subitems" ในแนวตั้งเพื่อสลับโหมดการนำเข้าและเชื่อมโยงเข้ากับ Item หลัก
- **Hybrid Columns:** คอลัมน์บางประเภทจะเปลี่ยนพฤติกรรมตามระดับชั้นข้อมูล:
  - `SOR Complete`: เป็น **Status** ใน Item หลัก และเป็น **Date** ใน Item ย่อย
  - `SOR File` / `RFI Sent`: เป็น **Status** ใน Item หลัก และเป็น **Text/File** ใน Item ย่อย

## 5. การบูรณาการข้อมูลอัปเดต (Updates Integration)
- **Sheet Discovery:** ค้นหาแผ่นงานที่มีชื่อ "Update", "อัพเดท", "อัปเดต", "Record"
- **ID Normalization:** กำจัดส่วนเกินของ ID ที่ Excel มักใส่มา (เช่น `.0`) เพื่อให้การ Join ข้อมูลระหว่าง Item และ Update มีความแม่นยำ 100%
- **Updates Mapping:** ข้อมูลอัปเดตจะถูกฝังลงในรายการ (Item) โดยตรงและแสดงผลในส่วน Conversation

## 6. ข้อกำหนดด้านความสวยงาม (UI/UX Guidelines)
- **Group Row:** บังคับให้หัวกลุ่ม (Group Title) แสดงผลบนบรรทัดเดียว (No Wrap / Single Line) และปรับความกว้างอัตโนมัติ (Auto-width)
- **Table:** นำขอบขวาสุด (Right-hand border) ของคอลัมน์สุดท้ายออก เพื่อความโปร่งและทันสมัยตาม NHG Style Premium
- **Header Sticks:** ตารางรองรับการตรึงหัวข้อกลุ่ม (Sticky Title) ขณะเลื่อนไปทางขวาเพื่อให้ไม่หลุดจากการมองเห็น
